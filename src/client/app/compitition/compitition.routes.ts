import { Route } from '@angular/router';
import{ CompititionComponent} from './compitition.component';

export const CompititionRoutes: Route[]=[
	{
		path: 'compititions',
    	component: CompititionComponent
	}
]